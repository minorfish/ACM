#include <cstdio>
#include <cstring>

const int maxn = 105;
const int dir[4][2] = {{0, -1}, {0, 1}, {1, 0}, {-1, 0}};

char G[maxn][maxn];
int f[maxn][manx];

int n, m;
int main () {
	
	while (scanf ("%d%d", &n, &m) != EOF) {

		for (int i = 0; i < n; i++)
			scanf ("%d
	}
	return 0;
}
